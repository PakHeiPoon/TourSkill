# Dependencies
node_modules/
venv/
__pycache__/
*.pyc

# Environment variables (NEVER commit)
.env
.env.local
.env.*.local

# Build outputs
dist/
dist-ssr/
build/
*.local

# IDE
.vscode/
.idea/
.trae/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# Hardhat / Solidity
contracts/cache/
contracts/artifacts/

# Seed scripts — kept in repo (used by seed_onchain_and_db.py as the canonical
# merchant catalog for on-chain registration + Supabase seeding).
# To re-seed: cd backend && python scripts/seed_onchain_and_db.py --wipe

# SQL schema — kept in repo so collaborators can set up Supabase locally.
# To apply: paste backend/sql/001_create_merchants.sql into Supabase SQL editor.

# Lock files (optional — uncomment if you want to track them)
# package-lock.json

# Raw design docs (internal only)
raw/
